eureka:
  client:
    enabled: false
spring:
  data:
    web:
      pageable:
        default-page-size: 50
  cloud:
    config:
      enabled: false
  liquibase:
    change-log: classpath:db/changelog/db.changelog-master-test.xml


